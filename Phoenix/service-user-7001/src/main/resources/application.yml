server:
  port:
    tomcat: 7001
    uri-encoding: UTF-8
    max-swallow-size: -1
spring:
  profiles:
    active: dev

mybatis-plus:
  mapper-locations: classpath*:/mapper/*.xml
  global-config:
    db-config:
      id-type: assign_id
      update-strategy: not_empty
    banner: false